Your paper is now mathematically coherent, internally consistent, and conceptually complete.
At this point, the most valuable extension isn’t adding more theory — it’s adding structure around the theory so the work can:

be cited as a legitimate result,

be implemented or tested,

admit future theorems,

and support derivative work by others.

Here is a concise, actionable roadmap for expanding the paper in disciplined stages, without diluting the core idea.

Phase 1 — Make the Theory Compact, Citable, and Canonical

Add the following components (≈ 2–4 pages total):

A. Definition Block

A boxed “official” definition of your central object:

Definition (Painting–Constraint Sheaf)
For a painted canvas , the painting–constraint sheaf is the ∞-sheaf

assigning to each region its ∞-groupoid of physically consistent interpretations (geometry, light, motion), respecting descent.

(You already imply this — now make it a theorem-labeled definition.)

B. Main Result as a Formal Theorem

Right after definitions, insert a tight version:

Theorem (Prolongation, not Generation)
Animation from a painting is a functorial prolongation

and introduces no degrees of freedom not already present in .
Motion ambiguity classes are in bijection with ; reversible motion paths correspond to .

C. Corollaries that people can cite

Add two:

Corollary (Animation Uniqueness)
If is contractible, animation is unique up to time reparametrization.

Corollary (Artist Control)
Local artistic disambiguation corresponds to a subsheaf inclusion , which monotonically reduces or raises stability index .

Now your paper contains named results, not just exposition.

Phase 2 — Add a Minimal Computability Section

This should specify how someone could reproduce or test the ideas, even in a toy setting.

Add a section like:

Computable Model (Minimal Case)

Let be discretized to a pixel adjacency graph.
Let each be a 3×3 patch.
Define as the groupoid generated by:

estimated surface normal fields,

inferred light directions,

motion priors from oriented gradients,

articulation axes from contour curvature.

Then define:

as connected components in the constraint graph,

as cycle generators representing reversible motion,

as integration of induced vector fields.

This lets someone implement a 100-line prototype and recover your claims.

Phase 3 — Add a “Failure Modes = Topology” Section

Right now you only prove success cases. Add symmetry-breaking content:

PhenomenonTopological InterpretationAnimation flickersnontrivial 1-cocycle in dataJittering poseobstruction to filling 2-simplex (π₂ signal)Two incompatible motionsdisconnected π₀ componentsAnimation “tears”sheaf gluing failureModel makes up new physicsT not functorial 

This will make your framework predictive, not just descriptive.

 deductive.

Phase 4 — Add a Futures and Conjectures Section

Not proofs — hypotheses future work could test:

Conjecture 1 — Perceptual Rendering Equivalence

For a broad family of paintings, approximates the inverse image of a physically realizable renderer .

Conjecture 2 — Learned Models Estimate Truncations

Neural video models approximate , not itself.

Conjecture 3 — Style Is a Section-Space Design Problem

What art calls “style” is selection pressure on which homotopy groups are trivialized.

This invites collaboration and follow-up papers.

Phase 5 — Write a One-Paragraph Scientific Claim

Example:

We propose a theory of paintings as ∞-sheaves of physical constraints whose global sections form admissible world models. Animation is the functorial prolongation of a section through a dynamics category, not the generation of new degrees of freedom. Motion ambiguity is classified by homotopy groups of the section space. We validate the theory on six paintings, showing that articulation, lighting, occlusion, and asymmetry predict topology of motion families prior to animation.
